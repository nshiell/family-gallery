{% extends 'base.html.twig' %}

{% block title %}RelativeAlias{% endblock %}

{% block body %}
    <h1>RelativeAlias</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Alias</th>
                <td>{{ relative_alias.alias }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('relative_alias_index') }}">back to list</a>

    <a href="{{ path('relative_alias_edit', {'user': relative_alias.user}) }}">edit</a>

    {{ include('relative_alias/_delete_form.html.twig') }}
{% endblock %}
